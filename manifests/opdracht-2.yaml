apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: opdracht-2
  labels:
    app: nginx
spec:
  restartPolicy: Never
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80

